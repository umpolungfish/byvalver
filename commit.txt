fix: Resolve vex_evx build error and harden CodeGenerationAgent prompt

Changes:
========

1. Generated Strategy Build Error Fix
   - vex_evx_prefix_byte_remapping_strategies.c
     * Removed conflicting static get_reg_index — core.h:72 declares it
       as a non-static global; redefining it static caused:
       "error: static declaration of 'get_reg_index' follows non-static
       declaration"
     * Removed unused static is_vex_evex_instruction
     * Removed unused static get_reg_class
     * Removed unused local variables bad_third_byte (generate_vex_remapping)
       and bad_byte2, bad_byte3 (generate_evex_remapping)
     * Zero-initialized EVEX field variables (evex_r, evex_x, evex_b,
       evex_r2, evex_mm, evex_w, evex_vvvv, evex_pp, evex_z, evex_bc,
       evex_aaa) to suppress false "may be used uninitialized" warnings
       from GCC — values are always set via extract_evex_fields() pointer
       params before use

2. CodeGenerationAgent Prompt Hardening
   - agents/code_generation_agent.py
     * Added hard constraint covering core.h functions alongside the
       existing utils.h constraint — core.h is transitively included via
       strategy.h and declares get_reg_index, is_rip_relative_operand,
       is_relative_jump, and all fallback_* functions; redefining any of
       these as static triggers the same "static declaration follows
       non-static declaration" error class
     * Rewrote all hard constraints from passive/title-prefixed framing to
       second-person authoritative framing throughout — every constraint
       now opens with a direct "You MUST" / "You MUST NOT" declaration and
       uses EXPRESSLY PROHIBITED for categorical prohibitions

Validated: make — zero errors, zero warnings on strategy object files
(pre-existing tui/ permission error unrelated).
